# Netlify configuration file for deploying the research group website
# Copy this file to the root of your project

[build]
  command = "npm run build"
  publish = "dist"

# Redirect all routes to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
